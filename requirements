(pip install
fastapi
uvicorn
sqlalchemy
psycopg2-binary
python-dotenv
anthropic
)
---------------------------installed
---------------------------

python3 -m venv .venv  (env)
source .venv/bin/activate (activate)

uvicorn main:app --reload
http://127.0.0.1:8000/docs

-------------------------------
(pip install
google-auth
google-auth-oauthlib
httpx 
python-jose[cryptography]
passlib[bcrypt]
)
-------------------------------installed
-------------------------------

sudo -u postgres psql
\c ctf


GRANT ALL PRIVILEGES ON DATABASE ctf TO aya;
GRANT ALL ON SCHEMA public TO aya;
ALTER SCHEMA public OWNER TO aya;

DROP TABLE table_name CASCAD;

i will use this url to get the code from google
https://accounts.google.com/o/oauth2/v2/auth?client_id=874710150809-0k2e3o85fulq1btgnkm2hdmc2uimivcl.apps.googleusercontent.com&redirect_uri=http://localhost:5173&response_type=code&scope=openid%20email%20profile&access_type=offline&prompt=consent

to create secret-key
python3 -c "import secrets; print(secrets.token_hex(32))"

================================

(my first main file before docker because the app is depend on the db and i need to check first the db is running)
from fastapi import FastAPI
from core.database import engine, Base
from api.auth_api import router as auth_router

import models

app = FastAPI()

Base.metadata.create_all(bind=engine)

app.include_router(auth_router)


